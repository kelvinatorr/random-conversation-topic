{
  "name": "random-conversation-topic",
  "version": "0.0.1",
  "description": "Randomly pick a topic for conversation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server ./app",
    "serve:dist": "gulp build && http-server ./dist",
    "build": "gulp build",
    "deploy": "gulp build && firebase deploy"
  },
  "author": "Kelvin Dealca",
  "license": "ISC",
  "devDependencies": {
    "del": "^2.2.2",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-htmlmin": "^3.0.0",
    "gulp-minify-css": "^1.2.4",
    "gulp-minify-inline": "^0.2.1",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^2.0.0",
    "gulp-vulcanize": "^6.1.0",
    "merge-stream": "^1.0.0",
    "run-sequence": "^1.2.2"
  }
}
